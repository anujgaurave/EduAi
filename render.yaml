services:
  - type: web
    name: eduai-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements-web.txt
    startCommand: python run.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
